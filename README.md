# 🏗️ Angular Sandbox for Master’s Course: Fundamental Algorithms

This project is a **personal sandbox** for practicing and experimenting with algorithms in **TypeScript** using Angular.  
It provides a convenient and interactive environment to implement, test, and explore various algorithms without any complex setup.

---
## 💻 Installation

To run this project locally, make sure you have the following installed:

- **[Node.js](https://nodejs.org/)** – simply download and install it on your computer.  
- **[Angular CLI](https://angular.io/cli)** – install via npm with the following command:  
  ```bash
  npm install -g @angular/cli

Then follow these steps:

```bash
# Clone the repository
git clone https://github.com/ArturDavidenko/FundamentalAlgorithmsSandbox.git

# Navigate to project folder
cd your-repo-name

# Install dependencies
npm install

# Run the project locally
ng serve

# Open your browser and navigate to:
http://localhost:4200


